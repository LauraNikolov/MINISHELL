
enum token {
	WORD,
	DOUBLE_Q,
	SIMPLE_Q,
	PIPE,
	LEFT_REDIR,
	RIGHT_REDIR
};

struct transition {
	state_t st;
	action_t ac;
};


enum state_t {

	START,
	COMMAND,


void parse(void) {

	token_list list = NULL;

	while (list) {



		list = list->next;
	}

}
